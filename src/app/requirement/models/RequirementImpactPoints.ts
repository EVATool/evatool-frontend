import { Dimension } from './Dimension';
import {Variants} from "./Variants";

export class RequirementImpactPoints{
  [k: string]: any;
  entityId!: any;
  impactDescription = '';
  points!: number | null;
}
